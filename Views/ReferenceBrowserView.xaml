<UserControl x:Class="ExchangeFileValidator.Views.ReferenceBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid Margin="24,8,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="2,0,2,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="280" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding Counts}" VerticalAlignment="Center" Opacity="0.8" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding SearchFields}" SelectedItem="{Binding SelectedSearchField}" Margin="0,0,6,0" />
            <ui:TextBox Grid.Column="2" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Search…" Icon="{ui:SymbolIcon Search24}" />
        </Grid>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding SignalsView}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  RowHeaderWidth="0" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Signal" Binding="{Binding SignalName}" Width="240" />
                <DataGridTextColumn Header="Frame" Binding="{Binding FrameName}" Width="160" />
                <DataGridTextColumn Header="ID" Binding="{Binding FrameIdHex}" Width="70" />
                <DataGridTextColumn Header="PDU" Binding="{Binding PduName}" Width="160" />
                <DataGridTextColumn Header="Bits" Binding="{Binding SignalSizeBits}" Width="55" />
                <DataGridTextColumn Header="Type" Binding="{Binding ValueType}" Width="90" />
                <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="70" />
                <DataGridTextColumn Header="Min" Binding="{Binding Min}" Width="70" />
                <DataGridTextColumn Header="Max" Binding="{Binding Max}" Width="70" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
