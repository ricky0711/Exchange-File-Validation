<UserControl x:Class="ExchangeFileValidator.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:views="clr-namespace:ExchangeFileValidator.Views">
    <UserControl.Resources>
        <views:StringToBrushConverter x:Key="StrToBrush" />
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="28,22,28,22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Validation Overview" FontSize="22" FontWeight="SemiBold" />
                <TextBlock Text="{Binding Headline}" Opacity="0.7" Margin="0,6,0,0" TextWrapping="Wrap" />
            </StackPanel>

            <!-- top KPI strip -->
            <ItemsControl Grid.Row="1" ItemsSource="{Binding Kpis}" Margin="0,0,0,8">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><UniformGrid Rows="1" /></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                                CornerRadius="12" Margin="0,0,14,8" Padding="0" ClipToBounds="True"
                                Effect="{StaticResource SoftShadow}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="6" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{Binding Color, Converter={StaticResource StrToBrush}}" />
                                <StackPanel Grid.Column="1" Margin="16,16,16,16">
                                    <TextBlock Text="{Binding Title}" Opacity="0.55" FontSize="12" />
                                    <TextBlock Text="{Binding Value}" FontSize="26" FontWeight="SemiBold" Margin="0,6,0,2"
                                               Foreground="{Binding Color, Converter={StaticResource StrToBrush}}" />
                                    <TextBlock Text="{Binding Detail}" Opacity="0.5" FontSize="11" TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>
