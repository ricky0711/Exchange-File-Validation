<UserControl x:Class="ExchangeFileValidator.Views.ExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:views="clr-namespace:ExchangeFileValidator.Views"
             xmlns:vm="clr-namespace:ExchangeFileValidator.ViewModels">
    <UserControl.Resources>
        <views:FunctionalToBrushConverter x:Key="FunctionalToBrush" />
        <views:IsrStatusToBrushConverter x:Key="IsrStatusToBrush" />

        <Style x:Key="Chip" TargetType="Border">
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="6,1" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="8,0,0,0" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="24,12,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Explorer" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0" />
            <TextBlock Text="Group by" Opacity="0.7" VerticalAlignment="Center" Margin="0,0,6,0" />
            <ComboBox Width="220" ItemsSource="{Binding GroupModes}" SelectedItem="{Binding GroupMode}" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding Summary}" VerticalAlignment="Center" Opacity="0.8" />
            <ui:TextBox Grid.Column="1" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Filter roots…" Icon="{ui:SymbolIcon Search24}" />
        </Grid>

        <Border Grid.Row="2" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                CornerRadius="8" Padding="6" Effect="{StaticResource SoftShadow}">
            <TreeView ItemsSource="{Binding RootsView}" BorderThickness="0" Background="Transparent"
                      VirtualizingStackPanel.IsVirtualizing="True"
                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <TreeView.ItemsPanel>
                    <ItemsPanelTemplate><VirtualizingStackPanel /></ItemsPanelTemplate>
                </TreeView.ItemsPanel>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                        <Setter Property="Padding" Value="2,3" />
                    </Style>
                </TreeView.ItemContainerStyle>

                <TreeView.Resources>
                    <!-- Loading placeholder -->
                    <DataTemplate DataType="{x:Type vm:LoadingNode}">
                        <TextBlock Text="Loading…" Opacity="0.5" FontStyle="Italic" />
                    </DataTemplate>

                    <!-- PDU -->
                    <HierarchicalDataTemplate DataType="{x:Type vm:PduNode}" ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center" />
                            <Border Style="{StaticResource Chip}" Background="{DynamicResource ControlFillColorSecondaryBrush}">
                                <TextBlock Text="{Binding CountText}" FontSize="11" Opacity="0.8" />
                            </Border>
                        </StackPanel>
                    </HierarchicalDataTemplate>

                    <!-- Frame -->
                    <HierarchicalDataTemplate DataType="{x:Type vm:FrameNode}" ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Meta}" Opacity="0.55" FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0" />
                            <Border Style="{StaticResource Chip}" Background="{DynamicResource ControlFillColorSecondaryBrush}">
                                <TextBlock Text="{Binding CountText}" FontSize="11" Opacity="0.8" />
                            </Border>
                            <ui:Button Content="Open frame view" Appearance="Transparent" Padding="8,2" FontSize="11" Margin="10,0,0,0"
                                       Command="{Binding DataContext.OpenFrameCommand, RelativeSource={RelativeSource AncestorType=TreeView}}"
                                       CommandParameter="{Binding}" />
                        </StackPanel>
                    </HierarchicalDataTemplate>

                    <!-- Signal -->
                    <HierarchicalDataTemplate DataType="{x:Type vm:SignalNode}" ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                            <Border Style="{StaticResource Chip}" Background="{Binding Functional, Converter={StaticResource FunctionalToBrush}}">
                                <TextBlock Text="func" Foreground="White" FontSize="10" FontWeight="SemiBold" />
                            </Border>
                            <TextBlock Text="{Binding Position}" Opacity="0.55" FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0" />
                            <Border Style="{StaticResource Chip}" Background="{DynamicResource ControlFillColorSecondaryBrush}">
                                <TextBlock Text="{Binding CountText}" FontSize="11" Opacity="0.8" />
                            </Border>
                        </StackPanel>
                    </HierarchicalDataTemplate>

                    <!-- ISR leaf -->
                    <DataTemplate DataType="{x:Type vm:IsrNode}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding IsrNumber}" VerticalAlignment="Center" />
                            <Border Style="{StaticResource Chip}" Background="{Binding Status, Converter={StaticResource IsrStatusToBrush}}">
                                <TextBlock Text="{Binding Status}" Foreground="White" FontSize="10" FontWeight="SemiBold" />
                            </Border>
                            <TextBlock Text="{Binding Route}" Opacity="0.6" FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0" />
                            <ui:Button Content="Open in trace" Appearance="Transparent" Padding="8,2" FontSize="11" Margin="10,0,0,0"
                                       Command="{Binding DataContext.OpenTraceCommand, RelativeSource={RelativeSource AncestorType=TreeView}}"
                                       CommandParameter="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </TreeView.Resources>
            </TreeView>
        </Border>
    </Grid>
</UserControl>
