<UserControl x:Class="ExchangeFileValidator.Views.ChecklistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:views="clr-namespace:ExchangeFileValidator.Views">
    <UserControl.Resources>
        <views:StatusToBrushConverter x:Key="StatusToBrush" />
    </UserControl.Resources>

    <Grid Margin="24,12,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <ui:Button Content="Run All Checks" Appearance="Primary" Command="{Binding RunCommand}" Margin="0,0,8,0" />
            <ui:Button Content="Export Report" Command="{Binding ExportReportCommand}" Icon="{ui:SymbolIcon DocumentArrowDown24}" Margin="0,0,12,0" />
            <TextBlock Text="Validation Checklist" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{Binding Summary}" Opacity="0.85" Margin="2,0,2,12" />

        <ListView Grid.Row="2" ItemsSource="{Binding ItemsView}" BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"
                                       Opacity="0.6" Margin="0,14,0,6" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0,2" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <!-- Whole row is a button: clicking jumps to the Validation page filtered to this check -->
                    <Button Cursor="Hand" HorizontalContentAlignment="Stretch"
                            Command="{Binding DataContext.OpenCheckCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                            CommandParameter="{Binding}"
                            ToolTip="Open the matching findings on the Validation page">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Card" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                        CornerRadius="6" Padding="14,10" Margin="0,3" Effect="{StaticResource SoftShadow}">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Card" Property="Background"
                                                Value="{DynamicResource CardBackgroundFillColorSecondaryBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="220" />
                                <ColumnDefinition Width="16" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" CornerRadius="4" Padding="8,3" HorizontalAlignment="Left" VerticalAlignment="Center"
                                    Background="{Binding Status, Converter={StaticResource StatusToBrush}}">
                                <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="SemiBold" FontSize="12" />
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="6,0,0,0">
                                <TextBlock Text="{Binding Name}" FontSize="13" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding Kind}" Opacity="0.5" FontSize="11" />
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="{Binding Result}" VerticalAlignment="Center"
                                       HorizontalAlignment="Right" Opacity="0.8" FontSize="12" />
                            <TextBlock Grid.Column="3" Text="›" VerticalAlignment="Center" HorizontalAlignment="Right"
                                       Opacity="0.35" FontSize="16" Margin="4,0,0,0" />
                        </Grid>
                    </Button>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
