<UserControl x:Class="ExchangeFileValidator.Views.ValidationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:views="clr-namespace:ExchangeFileValidator.Views">
    <UserControl.Resources>
        <views:SeverityToBrushConverter x:Key="SeverityToBrush" />
    </UserControl.Resources>

    <Grid Margin="24,8,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="0" Content="Run Validation" Appearance="Primary"
                       Command="{Binding RunCommand}" Margin="0,0,8,0" />
            <ComboBox Grid.Column="1" Width="120" ItemsSource="{Binding SeverityFilters}"
                      SelectedItem="{Binding SelectedSeverity}" Margin="0,0,8,0" />
            <ComboBox Grid.Column="2" Width="170" ItemsSource="{Binding RuleFilters}"
                      SelectedItem="{Binding SelectedRule}" />
        </Grid>

        <Grid Grid.Row="1" Margin="2,0,2,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="280" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding Summary}" VerticalAlignment="Center" Opacity="0.9" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding SearchFields}" SelectedItem="{Binding SelectedSearchField}" Margin="0,0,6,0" VerticalAlignment="Center" />
            <ui:TextBox Grid.Column="2" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Search…" Icon="{ui:SymbolIcon Search24}" VerticalAlignment="Center" />
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding RowsView}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  RowHeaderWidth="0" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Severity" Width="90" SortMemberPath="Severity">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="8,2" HorizontalAlignment="Left"
                                    Background="{Binding Severity, Converter={StaticResource SeverityToBrush}}">
                                <TextBlock Text="{Binding Severity}" Foreground="White" FontWeight="SemiBold" FontSize="11" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Rule" Binding="{Binding Rule}" Width="150" />
                <DataGridTextColumn Header="ISR Number" Binding="{Binding Isr}" Width="130" />
                <DataGridTextColumn Header="Signal" Binding="{Binding Signal}" Width="220" />
                <DataGridTextColumn Header="Finding" Binding="{Binding Message}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
