<UserControl x:Class="ExchangeFileValidator.Views.LevelAssignmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:views="clr-namespace:ExchangeFileValidator.Views">
    <UserControl.Resources>
        <views:LevelToBrushConverter x:Key="LevelToBrush" />
    </UserControl.Resources>

    <Grid Margin="24,8,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="260" />
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="0" Content="Assign Levels" Appearance="Primary"
                       Command="{Binding AssignCommand}" Margin="0,0,8,0" />
            <ui:Button Grid.Column="1" Content="Load 2nd Architecture (L2.1)"
                       Command="{Binding LoadSecondArchitectureCommand}" Margin="0,0,8,0" />
            <ComboBox Grid.Column="3" ItemsSource="{Binding LevelFilters}"
                      SelectedItem="{Binding SelectedFilter}" />
        </Grid>

        <Grid Grid.Row="1" Margin="2,0,2,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="280" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center">
                <TextBlock Text="{Binding Summary}" Opacity="0.9" />
                <TextBlock Text="{Binding SecondArchName, StringFormat='2nd architecture: {0}'}" Opacity="0.55" FontSize="11" />
            </StackPanel>
            <ComboBox Grid.Column="1" ItemsSource="{Binding SearchFields}" SelectedItem="{Binding SelectedSearchField}" Margin="0,0,6,0" VerticalAlignment="Center" />
            <ui:TextBox Grid.Column="2" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Search…" Icon="{ui:SymbolIcon Search24}" VerticalAlignment="Center" />
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding DemandsView}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  RowHeaderWidth="0" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Level" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="8,2" HorizontalAlignment="Left"
                                    Background="{Binding Level, Converter={StaticResource LevelToBrush}}">
                                <TextBlock Text="{Binding LevelLabel}" Foreground="White" FontWeight="SemiBold" FontSize="12" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="ISR Number" Binding="{Binding IsrNumber}" Width="130" />
                <DataGridTextColumn Header="Signal (Proposal)" Binding="{Binding ParameterProposal}" Width="220" />
                <DataGridTextColumn Header="Tx → Rx" Binding="{Binding Route}" Width="160" />
                <DataGridTextColumn Header="Frame" Binding="{Binding Frame}" Width="120" />
                <DataGridTextColumn Header="Basis" Binding="{Binding LevelNote}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
