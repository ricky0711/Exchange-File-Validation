<UserControl x:Class="ExchangeFileValidator.Views.PropertyFillView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:views="clr-namespace:ExchangeFileValidator.Views">
    <UserControl.Resources>
        <views:LevelToBrushConverter x:Key="LevelToBrush" />
    </UserControl.Resources>

    <Grid Margin="24,8,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="170" />
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="0" Content="Fill Properties" Appearance="Primary" Command="{Binding FillCommand}" />
            <ComboBox Grid.Column="2" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatus}" />
        </Grid>

        <Grid Grid.Row="1" Margin="2,0,2,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="280" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding Summary}" VerticalAlignment="Center" Opacity="0.9" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding SearchFields}" SelectedItem="{Binding SelectedSearchField}" Margin="0,0,6,0" VerticalAlignment="Center" />
            <ui:TextBox Grid.Column="2" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Search…" Icon="{ui:SymbolIcon Search24}" VerticalAlignment="Center" />
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding DemandsView}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  RowHeaderWidth="0" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Lvl" Width="58">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Left"
                                    Background="{Binding Level, Converter={StaticResource LevelToBrush}}">
                                <TextBlock Text="{Binding LevelLabel}" Foreground="White" FontWeight="SemiBold" FontSize="11" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Signal" Binding="{Binding ParameterProposal}" Width="200" />
                <DataGridTextColumn Header="Frame" Binding="{Binding FilledFrame}" Width="140" />
                <DataGridTextColumn Header="ID" Binding="{Binding FilledFrameId}" Width="60" />
                <DataGridTextColumn Header="PDU" Binding="{Binding FilledPdu}" Width="140" />
                <DataGridTextColumn Header="Bits" Binding="{Binding FilledBits}" Width="48" />
                <DataGridTextColumn Header="Min" Binding="{Binding FilledMin}" Width="55" />
                <DataGridTextColumn Header="Max" Binding="{Binding FilledMax}" Width="55" />
                <DataGridTextColumn Header="CRC/CLK" Binding="{Binding CrcClk}" Width="80" />
                <DataGridTextColumn Header="Status" Binding="{Binding FillStatus}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
